name: Docker

# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

on:
  workflow_dispatch:

env:
  Version: "1.29.1.1%20SnowDrop"

jobs:
  build:

    runs-on: windows-2019

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build Docker image
        run: docker build --build-arg VERSION="$ENV:Version" -t nginx-win-servercore .
      
      - name: Tag Docker image
        run: docker tag nginx-win-servercore:latest ${{ github.actor }}/nginx-win:ltsc2019-"$ENV:Version"

      - name: Push Docker image to Docker Hub
        run: docker push ${{ github.actor }}/nginx-win:ltsc2019-"$ENV:Version"
